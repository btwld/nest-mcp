{
  "name": "@nest-mcp/example-sse-server",
  "version": "0.1.7",
  "private": true,
  "description": "Example MCP server using SSE transport with forRootAsync configuration",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "start:dev": "tsc --outDir dist && node dist/main.js",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@nest-mcp/common": "workspace:*",
    "@nest-mcp/server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.10.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
